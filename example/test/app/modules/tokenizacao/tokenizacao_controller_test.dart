import 'package:flutter_test/flutter_test.dart';

void main() {
  //initModule(TokenizacaoModule());
  // TokenizacaoController tokenizacao;
  //
  setUp(() {
    //     tokenizacao = TokenizacaoModule.to.get<TokenizacaoController>();
  });

  group('TokenizacaoController Test', () {
    //   test("First Test", () {
    //     expect(tokenizacao, isInstanceOf<TokenizacaoController>());
    //   });

    //   test("Set Value", () {
    //     expect(tokenizacao.value, equals(0));
    //     tokenizacao.increment();
    //     expect(tokenizacao.value, equals(1));
    //   });
  });
}
